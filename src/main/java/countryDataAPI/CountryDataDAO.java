package countryDataAPI;

public class CountryDataDAO {

    //data access object for the country data api.
    //basically this just needs getters and setters for each of the tables we will have in the db
    //in a larger project we would do multiple DAOs for each table potentially.  but we should only have 2 tables

    //during phase one of the work,  we will define the getters and setters to
}
